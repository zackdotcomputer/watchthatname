// import { domainResults } from "./search";
// import type { StandardScenario } from "./search.scenarios";

// describe("search", () => {
//   scenario("returns all domainResults", async (scenario: StandardScenario) => {
//     const result = await domainResults();

//     expect(result.length).toEqual(Object.keys(scenario.domainResult).length);
//   });
// });
